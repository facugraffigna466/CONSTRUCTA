# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
alembic==1.13.3
asyncpg==0.29.0

# Validation & config
pydantic==2.9.2
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Auth
bcrypt==4.2.1
PyJWT==2.9.0

# ── Phase 2 ────────────────────────────────────────────────────
twilio==9.3.4
apscheduler==3.10.4
python-socketio[asyncio_client]

# ── Observabilidad ─────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0

# ── Emails (transaccionales via Brevo) ─────────────────────────
# tenacity: retry con backoff exponencial ante 429/503/timeouts de Brevo.
# httpx ya está listado como dependencia general (línea 35).
tenacity>=9.0

# ── Phase 3 (uncomment when implementing) ──────────────────────
# anthropic==0.40.0
# openai-whisper==20231117

# ── Dev & testing ───────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
aiosqlite==0.22.1
openpyxl>=3.1.0

# Generación del IPI en .docx (docs/ipi/build_ipi_docx.py)
python-docx==1.2.0
